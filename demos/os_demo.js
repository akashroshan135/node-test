const os = require("os");

// platform
console.log(os.platform());

// cpu architecture
console.log(os.arch());

// cpu core stuff
console.log(os.cpus());

// free memory
console.log(os.freemem());

// total memory
console.log(os.totalmem());

// this is kinda boring but i dont wanna leave anything that might be important or something later when i wanna do some cool shit with this
